on:
  push:
    paths:
      - 'físicas/index.xml'
      - '.github/workflows/personas físicas (WADL).yml'
jobs:
  build:
    runs-on: ubuntu-latest 
    steps:
    - name: Interpretación de la definición del tipo de recurso
      run:
        echo "Hello, World!"
