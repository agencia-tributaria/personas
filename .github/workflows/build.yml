on: [push]
jobs:
  build:
    runs-on: ubuntu-latest 
    steps:
    - id: get-changed
      uses: jitterbit/get-changed-files@v1
      with:
        format: 'csv'
    - run: |
        echo ${{ steps.get-changed.outputs }}
    - uses: agencia-tributaria/resource_type@main
      with:
        changes: test
